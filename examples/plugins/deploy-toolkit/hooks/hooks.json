{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.command' | grep -qE '^(npm run deploy|docker push|kubectl apply)' && echo 'Deploy command detected - logging' >> ${HOME}/.claude/deploy-audit.log || true"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/log-deployment.sh"
          }
        ]
      }
    ]
  }
}
